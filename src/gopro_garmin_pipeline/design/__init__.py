"""Design system — single source of truth for colors, typography, and lockups.

See `design/tokens.json` for the raw definitions and `design/tokens.py`
for the Python loader. Consumed by `burn_overlay.py` and `intro_outro.py`.
"""
